sudo: required
services:
  - docker

before_install:
  - docker build -t vontronje/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true vontronje/docker-react npm run test -- --coverage
